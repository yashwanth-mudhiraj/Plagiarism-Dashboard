"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CharTokenizer = void 0;
const region_1 = require("../util/region");
const tokenizer_1 = require("./tokenizer");
class CharTokenizer extends tokenizer_1.Tokenizer {
    /**
     * Runs the parser on a given string. Returns an async iterator returning
     * tuples containing the stringified version of the token and the
     * corresponding position.
     *
     * @param text The text string to parse
     */
    *generateTokens(text) {
        for (const [lineNumber, line] of text.split("\n").entries())
            yield* line
                .split("")
                .map((char, col) => this.newToken(char, new region_1.Region(lineNumber, col, lineNumber, col + 1)));
    }
}
exports.CharTokenizer = CharTokenizer;
//# sourceMappingURL=charTokenizer.js.map