import { Token, Tokenizer } from "./tokenizer";
/**
 * An empty tokenizer, only to be used when no tokenization needs to happen in the index.
 */
export declare class EmptyTokenizer extends Tokenizer {
    /**
       * Does not do anything
       *
       * @param _text
       */
    generateTokens(_text: string): IterableIterator<Token>;
}
//# sourceMappingURL=emptyTokenizer.d.ts.map