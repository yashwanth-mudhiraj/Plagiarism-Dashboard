import { TokenHash } from "./tokenHash";
export interface Fingerprint {
    data: Array<string> | null;
    hash: number;
    start: number;
    stop: number;
}
export declare abstract class HashFilter {
    protected hasher: TokenHash;
    protected readonly kgramData: boolean;
    protected constructor(kgramData?: boolean);
    hashTokens(tokens: string[]): AsyncGenerator<[number, string]>;
    abstract fingerprints(tokens: string[]): AsyncIterableIterator<Fingerprint>;
}
//# sourceMappingURL=hashFilter.d.ts.map