import { Report } from "./lib/analyze/report";
import { CustomOptions, Options } from "./lib/util/options";
import { File } from "./lib/file/file";
export declare class Dolos {
    readonly options: Options;
    private readonly tokenizer;
    private readonly index;
    constructor(customOptions?: CustomOptions);
    analyzePaths(paths: string[]): Promise<Report>;
    analyze(files: Array<File>): Promise<Report>;
}
//# sourceMappingURL=dolos.d.ts.map